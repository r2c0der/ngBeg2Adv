import { ZippyDirective } from './zippy.directive';

describe('ZippyDirective', () => {
  it('should create an instance', () => {
    const directive = new ZippyDirective();
    expect(directive).toBeTruthy();
  });
});
